/** Estado de carregamento da rota {{moduleSlug}} (App Router). */
export default function Loading() {
  return (
    <div className="text-center py-16 text-gray-400 text-sm">
      Carregando {{moduleSlug}}...
    </div>
  );
}
